{
  "name": "musites-frontend",
  "version": "0.0.4",
  "private": false,
  "files": [
    "out"
  ],
  "dependencies": {
    "@mdi/js": "^6.7.96",
    "@mdi/react": "^1.6.0",
    "infrastry": "^0.0.2",
    "musites": "^0.0.4",
    "next": "10.2.3",
    "next-transpile-modules": "^7",
    "react": ">=16.8.0",
    "react-dom": ">=16.8.0",
    "typescript": "^4"
  },
  "scripts": {
    "dev": "cross-env NODE_OPTIONS=--openssl-legacy-provider next dev",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider next build && cross-env NODE_OPTIONS=--openssl-legacy-provider next export",
    "start": "cross-env NODE_OPTIONS=--openssl-legacy-provider next start"
  }
}
